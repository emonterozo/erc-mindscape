<div class="container h-100">
  <div class="row h-100 justify-content-center align-items-center">
    <div class="col-13">
      <div class="form-group text-center">
        <h3>Please enter the 4-digit verification code we sent via SMS:</h3>
        <span>(we send the code {{code}} to <strong>{{number}}</strong>)</span>
        <form action="/verifycode" method="post">
            <div>
                <input type="text" name="number" hidden="true" value={{number}}>
                <input name="code" type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
                <input name="code" type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
                <input name="code" type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
                <input name="code" type="text" maxLength="1" size="1" min="0" max="9" pattern="[0-9]{1}" />
            </div>
            <button class="btn btn-primary btn-embossed mt-2">Verify</button>
        </form>
      </div>
      <div class="form-group text-center">
        Didn't receive the code?<br />
        <a href="#">Send code again</a><br/>
      </div>
    </div>   
  </div>
</div>